# Structure

```
.
├── pages       For pages, that are imported to `slides.md`
│   └── ...md
├── slides.md   Entrypoint markdown file for VitePress
└── snippets    For code snippets (e.g. JS, TS code) that will be imported to pages/
    └── ...md
```
